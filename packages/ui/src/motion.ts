/**
 * Top-level motion barrel — mirrors the path called out in the design brief.
 *
 * Internally, motion is split across `src/motion/{variants,reduced-motion}.ts`;
 * this file is the public re-export.
 */

export * from './motion/index';
